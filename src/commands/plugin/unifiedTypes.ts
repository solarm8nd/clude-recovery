import MAGIC_PROXY, { createMagicProxy } from '../../recovery/magicProxy.js';

const __defaultExport: any = MAGIC_PROXY;
export default __defaultExport;

export type UnifiedInstalledItem = any;
export const UnifiedInstalledItem: any = createMagicProxy('UnifiedInstalledItem');

export const __esModule = true;